package io.descoped.dc.api;

import io.descoped.dc.api.context.ExecutionContext;

public class ItemList implements Processor {

    @Override
    public ExecutionContext process(ExecutionContext input) {
        return null;
    }
}
